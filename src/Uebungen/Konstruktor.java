package Uebungen;

public class Konstruktor {
    /*
    * Der konstruktor ist IMMER die erste Methode eines Objektes die aufgerufen wird
    * er wird verwendet um Attribute eines Objekts zu initialisieren(bereitet das Objekt vor)
    * this ist eine Referenz auf das Objekt selbst */
}
